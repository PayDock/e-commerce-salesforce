<isscript>
    var isActive = pdict.selectedPowerboardPaymentInstrument ?
        pdict.selectedPowerboardPaymentInstrument.paymentMethod === 'POWERBOARD_WALLET_BUTTONS_GOOGLE_PAY' :
        false;
</isscript>

<div class="tab-pane ${isActive ? 'active' : ''} powerboard-wallet-buttons-google-pay-content" id="powerboard-wallet-buttons-google-pay-content" content-id="${paymentOption.ID}" role="tabpanel">
    <fieldset class="payment-form-fields">
        <!--- payment method is PowerBoard Wallet Buttons Google Pay --->
        <input type="hidden" class="form-control"
            name="${pdict.forms.billingForm.paymentMethod.htmlName}"
            value="POWERBOARD_WALLET_BUTTONS_GOOGLE_PAY"
        >
    </fieldset>

    <isif condition="${paymentOption.description}">
        <p>${paymentOption.description}</p>
    </isif>
</div>
