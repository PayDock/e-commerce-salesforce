<isscript>
    var isActive = pdict.selectedPowerboardPaymentInstrument ?
        pdict.selectedPowerboardPaymentInstrument.paymentMethod === 'POWERBOARD_CHECKOUT_BUTTON_AFTERPAY' :
        false;
</isscript>

<div class="tab-pane ${isActive ? 'active' : ''} powerboard-checkout-button-afterpay-content" id="powerboard-checkout-button-afterpay-content" content-id="${paymentOption.ID}" role="tabpanel" data-dwfrm-name="${pdict.forms.billingForm.powerboardToken.htmlName}">
    <div style="text-align: center; margin-bottom: 12px;">
        <img class="powerboard-option"
            src="${URLUtils.staticURL('/images/powerboard.png')}"
            width="260px"
            alt="${paymentOption.name}"
            title="${paymentOption.name}"
        />
    </div>

    <fieldset class="payment-form-fields">
        <!--- payment method is Checkout Button Afterpay --->
        <input type="hidden" class="form-control"
            name="${pdict.forms.billingForm.paymentMethod.htmlName}"
            value="POWERBOARD_CHECKOUT_BUTTON_AFTERPAY"
        >
    </fieldset>

    <isif condition="${paymentOption.description}">
        <p>${paymentOption.description}</p>
    </isif>
</div>
