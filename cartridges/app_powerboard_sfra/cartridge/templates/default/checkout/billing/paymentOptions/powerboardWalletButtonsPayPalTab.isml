<isscript>
    var isActive = pdict.selectedPowerboardPaymentInstrument ?
        pdict.selectedPowerboardPaymentInstrument.paymentMethod === 'POWERBOARD_WALLET_BUTTONS_PAYPAL' :
        false;
</isscript>

<li class="nav-item" data-method-id="${paymentOption.ID}">
    <a class="nav-link powerboard-wallet-buttons-paypal-tab ${isActive ? 'active' : ''}" data-toggle="tab" href="#powerboard-wallet-buttons-paypal-content" role="tab">
        <img class="powerboard-wallet-buttons-paypal-option"
            src="${paymentOption.image || URLUtils.staticURL('/images/PayPal.png')}"
            height="13"
            alt="${paymentOption.name}"
            title="${paymentOption.name}"
        />
        <label>
            ${paymentOption.name}
        </label>
    </a>
</li>

<div class="credit-card-selection-new">
    <div class="tab-content">
        <isinclude template="checkout/billing/paymentOptions/powerboardWalletButtonsPayPalContent" />
    </div>
</div>
